from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent.parent

# Define the absolute path to the catalog
CATALOG_PATH = PROJECT_ROOT / "catalog.yaml"

# Define the absolute path to the catalog
CONFIG_PATH = PROJECT_ROOT / "config.yaml"